import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.amanecer.saludrenal',
  appName: 'Amanecer Salud Renal',
  webDir: 'www'
};

export default config;
